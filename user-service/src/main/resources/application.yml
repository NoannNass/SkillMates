spring.config.import: "configserver:"

spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
# Optionnel : profil par défaut
# spring:
#   profiles:
#     active: default

